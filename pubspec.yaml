name: audio_trimmer
description: Audio Trimmer & Cutter App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffmpeg_kit_flutter_new_audio: ^2.0.0
  audio_waveforms: ^2.0.2
  just_audio: ^0.9.40
  file_picker: ^8.0.3
  flutter_bloc: ^8.1.5
  path_provider: ^2.1.3
  equatable: ^2.0.5
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
